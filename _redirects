# Cloudflare Pages routing
#
# Do not redirect `/pages/*.html`: those files are fetched directly by
# `js/router.js` as SPA partials during in-app navigation.
#
# Missing-route handling lives in `/functions/[[path]].js` for Cloudflare.
# Netlify and Vercel fall through to their native `404.html` handling.








// Canonical production origin for the marketing site. Single source of
// truth for metadata (OpenGraph/canonical URLs), deep-link QR codes, and
// the trusted-host check on sensitive token pages (reset-password,
// verify-email) — see `trusted-host.ts`.
export const SITE_URL = "https://simphonia.pt";

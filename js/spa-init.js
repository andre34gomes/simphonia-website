/**
 * Simphonia — SPA Mode Flag
 *
 * Sets the global __SPA_MODE flag so layout.js and the router know to use
 * absolute paths for all page partials. Must load before router.js.
 */
window.__SPA_MODE = true;

